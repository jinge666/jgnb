// 懒加载路由
const <%= data.name %> = () => import(`./${<%= data.name %>}`)
export default {
  path: `/${<%= data.name %>}`,
  name: '<%= data.name %>',
  component: <%= data.name %>,
  children: [
  ]
}
